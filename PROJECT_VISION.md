# PROJECT VISION — 2026_2TeamProject

> **문서 목적**  
> 이 문서는 2026_2TeamProject가 **무엇을 만들고 있는지**, **어떤 방향으로 구현해야 하는지**, **기존 포켓몬 스타일 게임과 무엇이 다른지**를 팀 전체가 동일하게 이해하기 위한 기준 문서다.  
> 기능을 새로 설계하거나 기존 시스템을 수정하기 전에 이 문서를 먼저 읽는다.
>
> **기준 시점:** 2026-09-23 `main`  
> **개발 환경:** Unity 6000.3.23f1 / C# / 2D / 싱글플레이  
> **코드 작성 기준:** `ScriptRules.md`  
> **Codex 작업 기준:** `AGENTS.md.txt`

---

# 1. 프로젝트 한 문장 정의

이 프로젝트는 **옛날 2D 픽셀 포켓몬 게임처럼 이해하기 쉬운 턴제 전투 구조를 기반으로 하되, 포켓몬을 수집하고 성장시키는 게임이 아니라 `캐릭터 + 무기 + 에테르`의 조합과 플레이어가 직접 선택하는 성장 방식으로 자신만의 전투 빌드를 만드는 2D 픽셀 턴제 RPG**를 목표로 한다.

핵심은 포켓몬을 그대로 재현하는 것이 아니다.

우리가 가져오려는 것은 다음과 같은 **턴제 전투의 장점**이다.

- 한 턴마다 행동을 선택한다
- 스킬마다 속성, 위력, 명중률, PP, 우선도가 존재한다
- 물리 공격과 특수 공격을 구분한다
- 속도와 우선도에 따라 행동 순서가 결정된다
- 속성 상성을 이용해 유리한 선택을 만든다
- 전투 규칙을 플레이어가 쉽게 이해할 수 있다
- 적은 수의 선택으로도 전략적인 판단이 가능하다

하지만 전투원을 구성하고 성장시키는 방식은 우리 게임만의 구조를 사용한다.

---

# 2. 우리가 만들고자 하는 게임의 핵심 목표

## 2.1 익숙하지만 그대로 복제하지 않는 턴제 전투

플레이어가 전투 화면을 보았을 때는 옛날 포켓몬 게임처럼 빠르게 이해할 수 있어야 한다.

예를 들어 다음 흐름은 익숙하게 유지한다.

```text
내 행동 선택
↓
적 행동 선택
↓
행동 순서 결정
↓
첫 번째 행동 실행
↓
전투 종료 여부 확인
↓
두 번째 행동 실행
↓
다음 턴
```

그러나 내부의 캐릭터 성장과 전투 빌드는 포켓몬과 다르게 구성한다.

즉,

```text
포켓몬의 전투 이해도
+
RPG 장비 빌드
+
플레이어가 직접 정하는 능력치 성장
+
에테르를 통한 속성과 스킬 구성
```

을 결합하는 것이 기본 방향이다.

---

## 2.2 플레이어가 성장 방향을 직접 결정한다

포켓몬은 일반적으로 레벨이 오르면 해당 포켓몬의 종족값, 개체값, 노력치, 성격 등의 규칙을 기반으로 능력치가 자동으로 증가한다.

우리 게임은 이 구조를 그대로 사용하지 않는다.

우리 게임에서는 캐릭터가 레벨업하면 **스탯 포인트를 지급받고**, 플레이어가 그 포인트를 원하는 능력치에 직접 투자하는 방향을 사용한다.

예:

```text
레벨 업
↓
스탯 포인트 획득
↓
플레이어가 직접 투자할 능력치를 선택
↓
선택한 능력치 성장
```

투자 대상은 다음 전투 능력치를 기준으로 한다.

- HP
- 공격
- 방어
- 특수공격
- 특수방어
- 스피드

따라서 같은 캐릭터라도 플레이어의 선택에 따라 전혀 다른 전투 성향을 가질 수 있어야 한다.

예:

```text
같은 캐릭터 A

플레이어 1
→ 공격과 스피드 집중
→ 빠른 물리 공격형

플레이어 2
→ HP와 방어 집중
→ 생존 중심 방어형

플레이어 3
→ 특수공격 집중
→ 에테르 특수 스킬 중심
```

**레벨업 1회당 지급하는 스탯 포인트의 개수는 아직 확정하지 않는다.**

확정되지 않은 수치를 코드에서 임의로 결정하지 않는다.

---

# 3. 우리 게임에서 전투 캐릭터가 만들어지는 방식

우리 게임의 전투원은 하나의 데이터만으로 완성되지 않는다.

기본 개념은 다음과 같다.

```text
Character
+
Weapon
+
Aether
+
Skill
↓
BattleState
↓
실제 전투
```

각 데이터는 서로 다른 책임을 가진다.

---

# 4. Character의 역할

## 4.1 Character는 전투원의 기본 몸체다

`CharacterData`는 캐릭터의 기본 능력치 원본을 담당한다.

현재 `main`의 `CharacterData`에는 다음 값이 존재한다.

- Character ID
- Character Name
- Max HP
- Attack
- Defense
- Special Attack
- Special Defense
- Speed

즉 Character는 전투에서 사용할 **기본 능력치의 출발점**이다.

Character 자체가 에테르의 속성이나 모든 스킬을 가지고 있는 구조로 만들지 않는다.

---

## 4.2 장기적인 Character 성장 방향

프로젝트의 목표 구조에서는 Character 성장에 다음 요소가 추가될 수 있다.

- Level
- Experience
- 플레이어가 투자한 스탯 포인트
- 성장한 최종 기본 능력치

현재 `main`의 `CharacterData`에는 Level과 Experience가 아직 들어가 있지 않다.

따라서 이 문서에서 Level/Experience는 **확정된 게임 방향이지만 아직 현재 데이터 구조에 완전히 반영되지 않은 기능**으로 구분한다.

---

# 5. Weapon의 역할

## 5.1 Weapon은 모든 전투 능력치를 보정할 수 있다

무기는 단순한 공격력 장비가 아니다.

우리 게임의 Weapon은 다음 모든 전투 능력치에 영향을 줄 수 있다.

- HP
- 공격
- 방어
- 특수공격
- 특수방어
- 스피드

현재 `main`의 `WeaponData`는 각 능력치에 대해 `Multiplier` 값을 가지고 있다.

현재 필드 기준:

```text
healthMultiplier
attackMultiplier
defenseMultiplier
specialAttackMultiplier
specialDefenseMultiplier
speedMultiplier
```

따라서 현재 구현 방향에서는 Weapon이 Character의 능력치를 배율 방식으로 보정하는 데이터 구조를 사용하고 있다.

---

## 5.2 Weapon은 전용 스킬 1개를 가진다

Weapon에는 전용 `SkillData` 슬롯 1개가 있다.

따라서 Weapon의 역할은 두 가지다.

```text
Weapon
├─ 캐릭터 능력치 보정
└─ 무기 전용 스킬 제공
```

이 구조는 우리 게임을 포켓몬과 크게 구분하는 요소다.

포켓몬의 일반적인 기술 구성에서는 전투 캐릭터 하나가 자신의 기술 목록을 직접 가진다.

우리 게임에서는 스킬의 출처가 분리된다.

```text
Aether → 에테르 스킬
Weapon → 무기 전용 스킬
```

따라서 어떤 Weapon과 어떤 Aether를 함께 사용하는지가 전투 빌드를 결정하는 중요한 요소가 된다.

---

# 6. Aether의 역할

## 6.1 Aether는 속성과 스킬 구성을 담당한다

`AetherData`는 다음 정보를 가진다.

- Aether ID
- Aether Name
- Element Type
- SkillData 목록

현재 `main`에서는 Aether의 스킬 슬롯을 `SkillData[3]`으로 사용한다.

즉 하나의 Aether는 현재 구조상 **최대 3개의 에테르 스킬**을 제공한다.

```text
Aether
├─ 속성
├─ Skill 1
├─ Skill 2
└─ Skill 3
```

---

## 6.2 Aether는 Character의 기본 능력치를 소유하지 않는다

Character와 Aether의 책임을 섞지 않는다.

```text
Character
→ 기본 전투 능력치

Aether
→ 속성 + 에테르 스킬

Weapon
→ 능력치 보정 + 무기 전용 스킬
```

이 구분은 프로젝트 전체에서 유지한다.

Aether가 Character의 HP, Attack, Defense 등의 기본 능력치를 대신 소유하는 구조로 변경하지 않는다.

---

# 7. Skill의 역할

`SkillData`는 실제 전투 행동의 원본 데이터다.

현재 `main` 기준으로 다음 값을 가진다.

- Skill ID
- Skill Name
- Element Type
- Attack Type
- Damage / Power
- PP
- Accuracy
- Priority
- Effect ID

현재 공격 타입은 다음 두 종류다.

```text
Physical
Special
```

Physical 스킬은 공격과 방어 능력치를 사용한다.

```text
공격자 Attack
vs
방어자 Defense
```

Special 스킬은 특수공격과 특수방어 능력치를 사용한다.

```text
공격자 SpecialAttack
vs
방어자 SpecialDefense
```

`effectId`는 버프, 디버프, 상태이상 등의 확장을 위한 데이터지만 현재 전투 처리에 모든 효과가 구현된 상태라고 가정하지 않는다.

---

# 8. 최종적인 전투 빌드 구조

우리 게임의 전투 빌드는 다음처럼 생각한다.

```text
Character
기본 능력치와 성장 방향
        +
Weapon
모든 능력치 보정 + 전용 스킬 1개
        +
Aether
속성 + 에테르 스킬 최대 3개
        ↓
플레이어의 실제 전투 구성
```

따라서 목표 구조에서는 전투에서 사용할 스킬 선택지가 다음 두 출처에서 만들어진다.

```text
Aether Skill 최대 3개
+
Weapon Skill 1개
```

중요한 점은 **Character가 모든 것을 가지고 있는 것이 아니라 여러 시스템의 조합으로 하나의 전투 빌드가 만들어진다는 것**이다.

---

# 9. 이 구조가 중요한 이유

같은 Character라도 장비와 Aether가 바뀌면 전투 방식이 달라질 수 있다.

예시:

```text
Character A
공격 80
특수공격 80
스피드 70
```

### 구성 1

```text
공격 보정 Weapon
+
Fire Aether
+
물리 스킬 중심
```

결과:

```text
빠르게 높은 물리 데미지를 주는 빌드
```

### 구성 2

```text
특수공격 보정 Weapon
+
Water Aether
+
특수 스킬 중심
```

결과:

```text
같은 Character지만 특수 공격 중심 빌드
```

### 구성 3

```text
HP / 방어 보정 Weapon
+
상대 약점을 찌르는 Aether
```

결과:

```text
버티면서 상성으로 싸우는 빌드
```

즉 Character 선택만으로 전략이 끝나지 않는다.

**Character 성장 + Weapon + Aether + Skill 조합이 함께 전략을 만든다.**

---

# 10. 포켓몬 게임과 공통으로 사용하는 핵심 개념

우리는 포켓몬의 모든 시스템을 사용하는 것이 아니다.

턴제 전투에서 검증된 일부 개념을 참고한다.

## 공통적으로 참고하는 요소

| 요소 | 우리 게임 방향 |
|---|---|
| 턴제 전투 | 사용 |
| 스킬 선택 | 사용 |
| PP | 사용 |
| 명중률 | 사용 |
| 스킬 우선도 | 사용 |
| 스피드에 따른 선공 | 사용 |
| 물리 / 특수 구분 | 사용 |
| 속성 | 사용 |
| 속성 상성 | 사용 |
| 자속 보정 | 사용 |
| 급소 | 사용 |
| 랜덤 데미지 보정 | 사용하지 않음 |
| 기타 포켓몬 고유 보정 | 기본적으로 사용하지 않음 |

---

# 11. 포켓몬과 가장 큰 차이점

## 11.1 전투원의 구성 자체가 다르다

포켓몬에서는 일반적으로 한 포켓몬 안에 다음 요소가 강하게 묶여 있다.

```text
포켓몬
├─ 능력치
├─ 타입
├─ 기술
└─ 성장
```

우리 게임은 이를 분리한다.

```text
Character
→ 기본 능력치 / 성장

Weapon
→ 능력치 보정 / 전용 스킬

Aether
→ 속성 / 에테르 스킬

Skill
→ 실제 행동 데이터
```

따라서 우리의 핵심은 **하나의 존재를 키우는 것보다 여러 요소를 조합해 하나의 전투 빌드를 완성하는 것**에 더 가깝다.

---

## 11.2 레벨업 능력치가 자동 증가하지 않는다

포켓몬처럼 레벨업할 때 모든 능력치가 자동으로 정해진 규칙에 따라 성장하는 구조를 목표로 하지 않는다.

우리 게임은 플레이어가 얻은 스탯 포인트를 직접 분배한다.

이 차이 때문에 같은 캐릭터라도 플레이어마다 성장 결과가 달라질 수 있다.

---

## 11.3 Weapon이 전투 빌드의 핵심 요소다

우리 게임의 Weapon은 단순 외형이나 공격력 증가 아이템이 아니다.

모든 능력치에 영향을 줄 수 있고 전용 스킬까지 제공한다.

따라서 Weapon 변경은 실제 전투 방식의 변경으로 이어져야 한다.

---

## 11.4 Aether가 속성과 스킬을 분리해서 제공한다

Character 자체가 속성을 가지고 모든 스킬을 배우는 방향이 아니다.

Aether가 전투의 속성 정체성과 일부 스킬 구성을 담당한다.

이 구조를 이용하면 같은 Character가 다른 Aether를 사용했을 때 전혀 다른 상성 관계와 스킬 구성을 만들 수 있다.

---

## 11.5 포켓몬의 수집/육성 시스템을 그대로 복사하지 않는다

이 게임의 목표는 포켓몬 게임의 시스템 전체를 재현하는 것이 아니다.

별도 요구가 없는 한 다음 시스템을 포켓몬과 같다는 이유만으로 자동 추가하지 않는다.

- 포켓몬 포획 시스템
- 종족값
- 개체값
- 노력치
- 성격
- 교배
- 진화
- 특성
- 날씨
- 포켓몬식 상태이상 전체
- 포켓몬식 아이템 시스템 전체
- 포켓몬의 모든 타입
- 포켓몬의 모든 데미지 보정

필요한 시스템은 우리 게임의 목적에 맞게 별도로 결정한다.

---

# 12. 전투의 기본 진행 방향

현재 전투 시스템과 목표 흐름을 기준으로 한 턴은 다음과 같이 진행한다.

```text
[전투 시작]
↓
Player BattleState 생성
Enemy BattleState 생성
↓
[턴 시작]
↓
플레이어 행동 선택
↓
선택 가능한 스킬인지 확인
↓
PP 확인 및 소비
↓
적 행동 선택
↓
적 PP 확인 및 소비
↓
행동 순서 결정
↓
첫 번째 행동 실행
↓
명중 판정
↓
데미지 계산
↓
HP 적용
↓
사망 확인
↓
전투가 끝났으면 종료
↓
남은 두 번째 행동 실행
↓
사망 확인
↓
다음 턴
```

---

# 13. 행동 순서 결정 규칙

현재 `BattleTurnSystem`의 기준은 다음 순서다.

```text
1. Skill Priority
↓
2. Speed
↓
3. Priority와 Speed까지 같으면 50% 랜덤
```

즉,

```text
우선도가 높은 스킬
→ 먼저 행동

우선도가 같음
→ Speed가 높은 전투원 먼저 행동

Speed도 같음
→ 50% 확률로 선공 결정
```

이 랜덤은 **행동 순서를 정하기 위한 동률 처리**다.

데미지 랜덤 보정과는 다른 개념이다.

---

# 14. 명중률과 PP

## 14.1 명중률

Skill의 `Accuracy`를 사용하여 스킬 성공 여부를 결정한다.

현재 구현에서는 0~100 범위로 처리한다.

Accuracy가 100 이상이면 항상 명중한다.

---

## 14.2 PP

각 스킬은 최대 PP를 가진다.

`SkillData`는 원본 최대 PP를 가진다.

실제 전투 중 남은 PP는 `BattleState`에서 관리한다.

중요:

```text
SkillData.PP
→ 원본 데이터

BattleState Current PP
→ 전투 중 변경되는 상태
```

ScriptableObject의 PP 값을 전투 중 직접 감소시키지 않는다.

---

# 15. BattleState의 역할

`BattleState`는 전투 도중 변하는 값을 관리하는 런타임 상태다.

현재 `main`에서는 다음을 관리한다.

- CharacterData 참조
- AetherData 참조
- Current HP
- 스킬별 Current PP
- 사망 여부

장기적인 목표에서는 Weapon까지 적용한 **실제 전투용 최종 능력치**를 BattleState가 사용할 수 있도록 확장한다.

중요한 원칙:

```text
ScriptableObject
→ 원본 데이터

BattleState
→ 현재 전투에서 변하는 데이터
```

전투 도중 HP나 PP 때문에 원본 SO가 수정되어서는 안 된다.

전투가 끝났을 때 새로운 전투를 시작하면 필요한 런타임 상태를 다시 생성할 수 있어야 한다.

---

# 16. 능력치 데이터 흐름

목표 개념은 다음과 같다.

```text
Character 기본 능력치
↓
영구 성장 데이터 적용
- Level
- 투자한 Stat Point
↓
Weapon 능력치 보정
↓
전투 시작 시 최종 전투 능력치 생성
↓
BattleState가 사용
↓
전투 중 버프/디버프가 추가된다면 런타임에서 별도 적용
```

현재 아직 세부 성장 데이터의 저장 위치와 모든 보정 계산 순서는 완전히 확정되지 않았다.

따라서 확정되지 않은 세부 계산식을 임의로 코드에 고정하지 않는다.

---

# 17. 데미지 계산의 목표 규칙

현재 `BattleResolver` 안의 데미지 공식은 **임시 공식**이다.

현재 임시 구현:

```text
Skill Power × Attack / Defense
```

이 계산은 최종 전투 설계가 아니다.

최종적으로는 포켓몬식 기본 데미지 구조를 참고하되 우리 게임 규칙에 맞게 단순화한다.

---

## 17.1 기본 데미지

목표 기본 구조:

```text
기본 데미지
=
(((2 × Level / 5 + 2)
× Skill Power
× 공격 능력치 / 방어 능력치)
÷ 50)
+ 2
```

Physical Skill:

```text
공격 능력치 = Attack
방어 능력치 = Defense
```

Special Skill:

```text
공격 능력치 = SpecialAttack
방어 능력치 = SpecialDefense
```

세부적인 정수 처리 방식은 실제 최종 구현 시 하나의 규칙으로 통일한다.

---

# 18. 최종 데미지 계산 순서

우리 게임에서 데미지 보정 순서는 다음으로 고정한다.

```text
기본 데미지 계산
↓
자속 보정
↓
타입 상성
↓
급소 보정
↓
최종 데미지
```

순서를 임의로 변경하지 않는다.

---

## 18.1 자속 보정

사용한 스킬의 속성과 현재 Aether의 속성이 같으면 자속 보정을 적용한다.

개념:

```text
Skill.Type == Aether.AetherType
→ 자속 보정 적용
```

현재 목표 배율:

```text
일치 → ×1.5
불일치 → ×1.0
```

---

## 18.2 타입 상성

자속 보정 이후 공격 스킬의 타입과 상대의 타입을 비교한다.

기본 방향:

```text
효과가 좋음
→ 데미지 증가

보통
→ 그대로

효과가 나쁨
→ 데미지 감소
```

정확한 전체 타입 상성표와 모든 배율은 프로젝트의 타입 설계가 확정된 뒤 데이터로 관리한다.

현재 `ElementType`에는 다음 타입이 존재한다.

- Normal
- Fire
- Water
- Grass
- Electric
- Fighting

포켓몬에 타입이 존재한다는 이유만으로 모든 포켓몬 타입을 자동 추가하지 않는다.

---

## 18.3 급소

타입 상성 계산 이후 급소 보정을 적용한다.

우리 게임의 급소 데미지 배율은 다음으로 결정되어 있다.

```text
급소 발생
→ ×2.0
```

포켓몬 일부 세대/규칙에서 사용하는 ×1.5를 사용하지 않는다.

**급소가 발생할 확률과 세부 판정 규칙은 아직 별도로 확정해야 한다.**

확률이 확정되지 않은 상태에서 임의 값을 최종 규칙처럼 코드에 넣지 않는다.

---

## 18.4 사용하지 않는 데미지 보정

최종 데미지 계산에는 다음을 넣지 않는다.

```text
랜덤 데미지 보정
기타 포켓몬식 보정
```

즉 동일한 상태에서 동일한 공격을 하면 데미지 결과를 플레이어가 예측하기 쉬운 방향을 목표로 한다.

단, 명중 판정, 행동 순서 동률, 추후 확정될 급소 판정처럼 **명시적으로 존재하는 확률 요소**는 별개의 규칙이다.

---

# 19. 최종 데미지 흐름 예시

예:

```text
기본 데미지 = 100

Fire Aether가 Fire Skill 사용
→ 자속 ×1.5
→ 150

상대 타입에 효과가 좋음
→ 상성 ×2.0
→ 300

급소 발생
→ 급소 ×2.0
→ 600

최종 데미지 = 600
```

반대로 급소가 아니면:

```text
100
→ 자속 ×1.5 = 150
→ 상성 ×2.0 = 300
→ 급소 없음 ×1.0
→ 최종 300
```

이 예시는 **계산 순서를 설명하기 위한 예시**다.

---

# 20. 전투 종료 규칙

행동이 실행된 뒤 HP가 0 이하가 되면 해당 BattleState는 사망 상태다.

```text
CurrentHp <= 0
→ IsDead
```

첫 번째 행동으로 상대가 쓰러졌다면 전투 종료 여부를 먼저 확인한다.

이미 쓰러진 전투원이 예정된 두 번째 행동을 실행하도록 만들지 않는다.

---

# 21. 적 AI 방향

현재 `BattleFlow`의 적 스킬 선택은 **사용 가능한 첫 번째 스킬을 고르는 임시 방식**이다.

이것을 최종 AI라고 생각하지 않는다.

현재:

```text
Aether Skills를 앞에서부터 확인
↓
PP가 남은 첫 번째 Skill 선택
```

장기적으로 적 AI가 필요하다면 다음 요소를 고려할 수 있다.

- 사용 가능한 스킬
- PP
- 타입 상성
- 예상 데미지
- 현재 HP
- 스킬 우선도
- 상태 효과
- 보스별 행동 규칙

하지만 AI의 구체적인 난이도와 의사결정 방식은 별도 기획 없이 임의로 확장하지 않는다.

---

# 22. 데이터 관리 방향

전투 데이터는 코드에 직접 박아 넣는 방식보다 **데이터 중심 구조**를 사용한다.

현재 기본 흐름:

```text
CSV
↓
Unity Editor Importer
↓
ScriptableObject
↓
게임 실행
↓
BattleState
```

사용하는 대표 데이터:

```text
CharacterData.csv
→ CharacterData SO

WeaponData.csv
→ WeaponData SO

AetherData.csv
→ AetherData SO

SkillData.csv
→ SkillData SO
```

---

# 23. CSV와 ScriptableObject의 역할

## CSV

팀원이 많은 데이터를 표 형태로 수정하기 위한 원본 관리 수단이다.

예:

- 캐릭터 능력치
- 무기 보정값
- 스킬 위력
- PP
- 명중률
- 우선도
- Aether와 Skill 연결

## ScriptableObject

Unity 안에서 실제 게임이 읽기 쉬운 데이터 형태다.

런타임 전투는 매번 CSV 파일을 직접 검색해서 사용하지 않는다.

```text
CSV 수정
↓
Importer 실행
↓
SO 생성 / 갱신
↓
게임에서 SO 사용
```

---

# 24. 원본 데이터와 런타임 데이터를 섞지 않는다

예를 들어 다음은 잘못된 방향이다.

```text
SkillData.PP 자체를 전투 중 감소
CharacterData.MaxHp 자체를 데미지로 감소
WeaponData 배율 자체를 전투 중 변경
```

이 값들은 여러 전투에서 재사용하는 원본 데이터다.

전투 중 변하는 값은 `BattleState` 같은 런타임 객체가 관리한다.

---

# 25. UI 방향

UI의 시각적 방향은 **옛날 2D 픽셀 RPG / 포켓몬 계열의 명확하고 빠른 메뉴 조작감**을 참고한다.

하지만 화면과 메뉴를 그대로 복제하는 것이 목적은 아니다.

현재 프로젝트에서 정의한 주요 Navigation 기준은 다음과 같다.

- `Q` : Category UI 열기/닫기
- `A / D` : 좌우 선택
- `F` : Confirm
- `E` : Back
- Category UI만 Q 입력에 반응
- 하위 UI에서는 Q가 불필요하게 동작하지 않음
- Back은 이전 UI 기록을 따라 복귀
- Root에서 Back하면 UI 종료
- 캐러셀 이동 중에는 중복 Confirm 방지
- 이전 화면으로 돌아왔을 때 필요한 선택 상태 유지

UI 역시 전투와 마찬가지로 **입력을 복잡하게 만드는 것보다 적은 키와 명확한 상태 변화**를 우선한다.

---

# 26. 프로젝트의 핵심 재미

우리 게임의 핵심 재미는 단순히 강한 스킬을 누르는 것이 아니다.

다음 선택들이 서로 연결되는 것이 중요하다.

```text
어떤 Character를 사용하는가
↓
어떤 능력치에 성장 포인트를 투자했는가
↓
어떤 Weapon을 장착했는가
↓
어떤 Aether를 사용하는가
↓
어떤 Skill을 선택하는가
↓
상대 속성과 행동 순서를 어떻게 읽는가
```

즉 전투 전에 만든 빌드와 전투 중의 선택이 모두 결과에 영향을 줘야 한다.

---

# 27. 우리가 원하는 전략성

좋은 전략성은 시스템이 복잡한 것과 같은 뜻이 아니다.

우리 게임은 다음을 목표로 한다.

### 플레이어가 이해할 수 있어야 한다

왜 먼저 공격했는지 알 수 있어야 한다.

```text
우선도 때문인지
Speed 때문인지
동률 랜덤 때문인지
```

왜 데미지가 높았는지 알 수 있어야 한다.

```text
공격 능력치
Skill Power
자속
타입 상성
급소
```

왜 빌드가 달라졌는지 알 수 있어야 한다.

```text
Stat 투자
Weapon
Aether
Skill
```

규칙이 보이지 않는 숨겨진 보정을 지나치게 많이 만들지 않는다.

---

# 28. 포켓몬과 비슷하게 보여도 구현 목적이 다른 부분

포켓몬 스타일을 참고하는 이유는 다음 때문이다.

- 턴제 전투 흐름이 직관적이다
- 적은 입력으로 전략을 만들기 좋다
- Pixel 2D UI와 잘 어울린다
- Skill / Type / Speed의 관계가 이해하기 쉽다

하지만 다음 사고방식은 피한다.

```text
"포켓몬에 있으니까 우리도 넣자"
```

대신 항상 다음을 먼저 본다.

```text
"이 기능이 우리 게임의 Character + Weapon + Aether 빌드를 더 재미있게 만드는가?"
```

답이 아니라면 추가하지 않는다.

---

# 29. 구현자가 잘못 이해하면 안 되는 것

## 잘못된 이해 1

```text
Aether = 포켓몬
```

아니다.

Aether는 현재 설계상 **속성과 에테르 스킬을 제공하는 전투 구성 요소**다.

---

## 잘못된 이해 2

```text
Character = 포켓몬의 이름만 바꾼 것
```

아니다.

Character는 기본 능력치와 성장의 중심이다.

Weapon과 Aether를 조합해서 실제 전투 빌드가 만들어진다.

---

## 잘못된 이해 3

```text
레벨업하면 모든 스탯이 자동으로 오른다
```

아니다.

우리 게임의 목표는 스탯 포인트를 받고 플레이어가 직접 투자하는 방식이다.

---

## 잘못된 이해 4

```text
Weapon은 Attack만 올린다
```

아니다.

Weapon은 HP를 포함한 모든 전투 능력치를 보정할 수 있다.

---

## 잘못된 이해 5

```text
Aether 스킬만 전투에서 사용한다
```

최종 목표에서는 Aether Skill과 Weapon 전용 Skill이 함께 전투 선택지를 구성한다.

---

## 잘못된 이해 6

```text
포켓몬 데미지 공식을 그대로 복사한다
```

아니다.

기본 구조는 참고하지만 우리 게임은 다음 차이가 있다.

- 랜덤 데미지 보정 없음
- 기타 불필요한 보정 없음
- 급소 배율 ×2.0
- 계산 순서 고정
- 우리 게임의 Character / Weapon / Aether 구조를 사용

---

# 30. 새 기능을 만들 때 판단 기준

새로운 전투 기능을 구현하기 전에 다음 순서로 판단한다.

```text
1. 이 기능이 우리 게임의 핵심 재미와 관련 있는가?
↓
2. Character / Weapon / Aether / Skill 중 어디에 속하는가?
↓
3. 기존 시스템이 이미 같은 책임을 가지고 있는가?
↓
4. 원본 데이터인가, 런타임 상태인가?
↓
5. 포켓몬을 단순 복사해서 추가하려는 기능은 아닌가?
↓
6. 현재 필요한 기능인가?
↓
7. 확정되지 않은 규칙을 임의로 결정하고 있지는 않은가?
↓
8. ScriptRules.md에 맞는 구조인가?
↓
9. 최소 변경으로 기존 시스템에 연결할 수 있는가?
```

---

# 31. 데이터 소유권 기준

| 데이터 | 주된 책임 |
|---|---|
| CharacterData | 캐릭터 기본 능력치 원본 |
| WeaponData | 능력치 보정 + 무기 Skill |
| AetherData | 속성 + Aether Skill |
| SkillData | Skill 자체의 전투 규칙 |
| BattleState | 전투 중 HP, PP, 최종 전투 상태 |
| BattleSystem | 플레이어/적 전투 상태와 행동 연결 |
| BattleTurnSystem | 턴 Phase와 행동 순서 |
| BattleResolver | 선택된 행동의 실제 해결 |
| BattleFlow | 플레이어 선택부터 턴 실행까지 전체 진행 연결 |

한 클래스가 다른 클래스의 책임까지 무분별하게 가져가지 않는다.

구체적인 코드 작성 규칙은 `ScriptRules.md`를 따른다.

---

# 32. 현재 main 구현 상태

이 문서에서 특히 중요한 부분이다.

**목표 설계와 현재 구현 완료 상태는 같지 않다.**

2026-09-23 `main` 기준으로 확인된 상태:

| 영역 | 현재 상태 |
|---|---|
| CharacterData 기본 6능력치 | 구현 |
| Character Level / Experience | 아직 CharacterData에 없음 |
| 플레이어 직접 Stat 투자 | 목표 방향, 전체 시스템 미완성 |
| Aether 속성 | 구현 |
| Aether Skill 3슬롯 | 데이터 구조 구현 |
| Weapon 모든 능력치 보정 데이터 | 구현 |
| Weapon 전용 Skill 1슬롯 | 데이터 구조 구현 |
| Weapon을 BattleSystem에 연결 | 아직 미반영 |
| Weapon 보정을 BattleState 능력치에 적용 | 아직 미반영 |
| BattleState Current HP | 구현 |
| BattleState Skill Current PP | 구현 |
| 플레이어 Skill 선택 | 구현 |
| 적 Skill 선택 | 임시 방식 구현 |
| Priority → Speed → 동률 50% | 구현 |
| Accuracy 명중 판정 | 구현 |
| Physical / Special 구분 | 구현 |
| 최종 데미지 공식 | 아직 미반영 |
| 자속 보정 | 아직 미반영 |
| 타입 상성 | 아직 미반영 |
| 급소 ×2.0 | 목표 규칙, 아직 미반영 |
| Skill effectId 실제 효과 | 전체 구현 전 |
| CSV → SO Importer | 구현 진행/사용 중 |

따라서 구현자는 **현재 코드가 임시인 부분을 최종 기획으로 해석하지 않는다.**

---

# 33. 현재 BattleSystem의 한계

현재 `BattleSystem`은 다음 데이터로 Player와 Enemy의 `BattleState`를 만든다.

```text
CharacterData
+
AetherData
```

아직 WeaponData를 받지 않는다.

따라서 최종 목표를 위해서는 이후 다음 구조로 확장하는 방향이 자연스럽다.

```text
CharacterData
+
WeaponData
+
AetherData
↓
BattleState
```

단, 실제 수정 시에는 최신 코드를 다시 확인하고 기존 API 영향을 분석한 뒤 최소 변경으로 연결한다.

---

# 34. 현재 BattleState의 한계

현재 `BattleState`의 Speed는 다음과 같다.

```text
Character.Speed
```

즉 Weapon의 `speedMultiplier`가 아직 적용되지 않는다.

Attack, Defense, SpecialAttack, SpecialDefense도 현재 데미지 계산에서는 Character 원본 값을 직접 사용한다.

최종적으로 Weapon이 실제 전투에 적용되면 이러한 전투 계산은 **Weapon 보정까지 반영한 전투용 능력치**를 사용해야 한다.

---

# 35. 현재 BattleResolver의 한계

현재 `BattleResolver`의 데미지는 임시 계산이다.

현재:

```text
Skill.Damage
×
Attack / Defense
```

최종 목표:

```text
기본 데미지
↓
자속
↓
타입 상성
↓
급소 ×2.0
↓
최종 데미지
```

따라서 현재 `CalculateDamage`를 기준으로 게임 밸런스를 확정하면 안 된다.

---

# 36. 아직 임의로 확정하면 안 되는 항목

다음 항목은 추가 기획 없이 숫자나 규칙을 임의로 최종 확정하지 않는다.

- 최대 레벨
- 레벨별 필요 경험치
- 레벨업당 스탯 포인트 지급량
- 각 스탯 투자 제한
- 스탯 포인트 저장 방식
- Weapon 보정의 최종 밸런스 범위
- 전체 타입 목록
- 전체 타입 상성표
- 타입별 최종 상성 배율
- 급소 발생 확률
- 급소 판정에 영향을 주는 별도 요소
- 버프 / 디버프 세부 규칙
- 상태이상 세부 규칙
- `effectId`의 전체 효과 목록
- 적 AI 난이도
- 보스 AI 규칙
- 전투 중 아이템 사용 규칙
- Aether 교체 규칙
- Weapon 교체 규칙
- 전투 보상
- 패배 패널티

이 값들이 필요해지면 팀에서 결정한 뒤 문서와 데이터 구조를 함께 갱신한다.

---

# 37. 구현하지 말아야 하는 방향

별도 요구가 없다면 다음 방향으로 프로젝트를 키우지 않는다.

- 멀티플레이 시스템
- Photon
- Fusion
- RPC
- NetworkBehaviour
- NetworkObject
- 불필요한 Singleton
- 모든 시스템을 관리하는 거대한 Manager
- 포켓몬 시스템의 무분별한 복제
- 런타임에서 CSV 전체 반복 검색
- 전투 중 ScriptableObject 원본 수정
- 임시 수치를 최종 기획으로 고정
- 같은 책임의 Battle 시스템 중복 생성
- 단순한 기능을 과도한 Interface와 BaseClass로 분리
- 현재 사용하지 않는 미래 기능을 미리 대량 구현

---

# 38. 기술적인 프로젝트 방향

이 프로젝트는 다음 기준을 유지한다.

```text
Unity 6000.3.23f1
C#
2D
Single Player
```

2D 기능은 기본적으로 다음 Unity 시스템을 사용한다.

```text
Collider2D
Rigidbody2D
Physics2D
RaycastHit2D
```

멀티플레이 구조를 추가하지 않는다.

---

# 39. 코드 설계와 게임 설계 문서의 역할 구분

`PROJECT_VISION.md`

```text
무엇을 만들 것인가
왜 이렇게 만드는가
포켓몬과 무엇이 다른가
전투 규칙의 방향은 무엇인가
```

`ScriptRules.md`

```text
코드를 어떻게 설계하고 작성할 것인가
```

`AGENTS.md.txt`

```text
Codex가 프로젝트에서 어떤 방식으로 작업할 것인가
```

세 문서는 역할이 다르다.

새 기능을 구현할 때는 세 문서를 함께 기준으로 사용한다.

---

# 40. 문서와 코드가 충돌할 때

다음 원칙을 사용한다.

## 경우 1 — 문서는 목표인데 코드가 아직 임시 구현

예:

```text
문서
→ 최종 데미지 공식 사용

현재 코드
→ 임시 Damage × Attack / Defense
```

이 경우 현재 코드를 최종 기획으로 판단하지 않는다.

문서의 목표 방향을 기준으로 이후 구현한다.

---

## 경우 2 — 팀에서 새로운 결정을 내림

새로운 팀 결정이 이 문서보다 최신이라면 최신 결정이 우선이다.

그 뒤 이 문서도 반드시 수정한다.

---

## 경우 3 — 문서에 없는 세부 규칙

임의로 중요한 기획을 확정하지 않는다.

필요하면 `미정`, `추후 확정`, `팀 결정 필요` 상태로 둔다.

---

# 41. 팀원이 기억해야 하는 핵심

이 프로젝트는 단순한 포켓몬 복제 게임이 아니다.

우리가 참고하는 것은 **직관적인 2D 픽셀 턴제 전투의 구조**다.

우리 게임의 실제 중심은 다음이다.

```text
Character 성장 선택
+
Weapon 능력치 보정과 전용 Skill
+
Aether 속성과 Skill 구성
+
Skill 선택
+
Priority / Speed
+
속성 상성
+
예측 가능한 데미지 규칙
```

이를 통해 **같은 Character라도 성장과 장비 조합에 따라 서로 다른 전투 방식이 만들어지는 게임**을 목표로 한다.

---

# 42. 최종 구현 판단 문장

새 기능을 만들 때 아래 질문에 답할 수 있어야 한다.

> **“이 기능은 플레이어가 Character를 어떻게 성장시키고, 어떤 Weapon과 Aether를 조합하며, 전투에서 어떤 Skill을 선택할지 고민하게 만드는가?”**

이 질문과 관계가 없다면 정말 필요한 기능인지 다시 확인한다.

---

# 43. 최종 요약

## 우리 게임이 포켓몬에서 참고하는 것

```text
2D 픽셀 턴제 전투의 직관성
Skill 선택
PP
Accuracy
Priority
Speed
Physical / Special
Element Type
자속
타입 상성
급소
```

## 우리 게임이 다르게 만드는 것

```text
Character가 기본 능력치와 성장의 중심
↓
레벨업 시 플레이어가 Stat Point 직접 투자
↓
Weapon이 HP 포함 모든 능력치 보정
↓
Weapon 전용 Skill 1개
↓
Aether가 Element Type과 Skill 최대 3개 제공
↓
Character + Weapon + Aether 조합으로 빌드 완성
↓
랜덤 데미지 보정 제거
↓
급소 배율 ×2.0
↓
기본 데미지 → 자속 → 타입 상성 → 급소 순서로 계산
```

## 우리가 가장 중요하게 생각하는 것

**익숙한 턴제 전투를 사용하되, 플레이어가 자신의 성장과 장비 조합을 직접 결정하고 그 선택이 실제 전투 방식의 차이로 이어지게 만드는 것.**

---

# 44. 문서 유지 규칙

게임의 핵심 방향이 변경되면 코드만 수정하고 이 문서를 방치하지 않는다.

다음 항목이 변경될 때는 `PROJECT_VISION.md`도 함께 확인한다.

- Character 성장 방식
- Weapon 역할
- Aether 역할
- Skill 구성
- 전투 행동 순서
- 데미지 공식
- 타입 시스템
- 핵심 UI 조작
- 데이터 관리 방식
- 포켓몬과 구분되는 핵심 시스템

이 문서는 단순 설명서가 아니라 **팀 전체가 같은 게임을 만들기 위한 기준 문서**로 유지한다.
